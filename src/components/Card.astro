---
import Button from "../components/ui/Button.astro";

interface Props {
  title: string;
  subtitle: string;
  media: string;
  mediaType?: string;
  buttonLabel: string;
  buttonHref?: string;
  style?: string;
}

const { title, subtitle, media, mediaType, buttonLabel, style, buttonHref } =
  Astro.props;
---

<!-- <main class="col-12 col-lg-4 mb-lg-0 mb-4" style={style}>
  <div class="card p-2" style="">
    <div class="p-3 img-wrapper">
      {
        mediaType === "video" ? (
          <video width="100%" autoplay muted loop>
            <source src={media} type="video/mp4" />
          </video>
        ) : (
          <img src={media} alt={title} class="img-fluid" />
        )
      }
    </div>
    <div class="card-body">
      <h5 class="card-title">{title}</h5>
      <p class="card-text">
        {subtitle}
      </p>
      <Button label={buttonLabel} variant="outlineWhite" />
    </div>
  </div>
</main>

<style>
  .card {
    background: var(--gradient-color);
    border: none;
    border-radius: 20px;
    transition: all 0.3s;
    color: var(--text-light-color);
  }

  .img-wrapper video {
    border-radius: 10px;
  }
</style> -->
<main class="col-12 col-lg-4 mb-4" style={style} data-aos="zoom-in">
  <div class="card p-2 d-flex flex-column h-100">
    <div class="p-3 img-wrapper">
      {
        mediaType === "video" ? (
          <video width="100%" autoplay muted loop>
            <source src={media} type="video/mp4" />
          </video>
        ) : (
          <img src={media} alt={title} class="img-fluid" />
        )
      }
    </div>
    <div class="card-body d-flex flex-column justify-content-between">
      <h5 class="card-title">{title}</h5>
      <p class="card-text">
        {subtitle}
      </p>
      <Button label={buttonLabel} variant="outlineWhite" href={buttonHref} />
    </div>
  </div>
</main>

<style>
  .card {
    background: var(--gradient-color);
    border: none;
    border-radius: 20px;
    transition: all 0.3s;
    color: var(--text-light-color);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 100%;
  }

  .img-wrapper video,
  .img-wrapper img {
    border-radius: 10px;
    width: 100%;
    height: auto;
  }

  .card-body {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    flex-grow: 1;
  }

  .card-title {
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }

  .card-text {
    flex-grow: 1;
    margin-bottom: 1rem;
  }
</style>

<!-- ---
interface Props {
  title: string;
  body: string;
  href: string;
}

const { href, title, body } = Astro.props;
---

<li class="link-card">
  <a href={href}>
    <h2>
      {title}
      <span>&rarr;</span>
    </h2>
    <p>
      {body}
    </p>
  </a>
</li>
<style>
  .link-card {
    list-style: none;
    display: flex;
    padding: 1px;
    background-color: #23262d;
    background-image: none;
    background-size: 400%;
    border-radius: 7px;
    background-position: 100%;
    transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.1);
  }
  .link-card > a {
    width: 100%;
    text-decoration: none;
    line-height: 1.4;
    padding: calc(1.5rem - 1px);
    border-radius: 8px;
    color: white;
    background-color: #23262d;
    opacity: 0.8;
  }
  h2 {
    margin: 0;
    font-size: 1.25rem;
    transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }
  p {
    margin-top: 0.5rem;
    margin-bottom: 0;
  }
  .link-card:is(:hover, :focus-within) {
    background-position: 0;
    background-image: var(--accent-gradient);
  }
  .link-card:is(:hover, :focus-within) h2 {
    color: rgb(var(--accent-light));
  }
</style> -->
